# Copy to .env and adjust for your environment.
# Never commit real secrets.

DATABASE_URL="postgresql://sensework:sensework@localhost:5432/sensework?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
NODE_ENV="development"
# Set to "true" behind HTTPS. Local HTTP docker should use "false".
COOKIE_SECURE="false"
